<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.kah.mapper.AdminUserMapper">

    <sql id="adminColumns">
        id, username, password_hash, status, display_name, is_super_admin, last_login_at, created_at, updated_at
    </sql>

    <select id="findByUsername" resultType="org.example.kah.entity.AdminUser">
        SELECT <include refid="adminColumns" />
        FROM admin_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="findById" resultType="org.example.kah.entity.AdminUser">
        SELECT <include refid="adminColumns" />
        FROM admin_user
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findAll" resultType="org.example.kah.entity.AdminUser">
        SELECT <include refid="adminColumns" />
        FROM admin_user
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="org.example.kah.entity.AdminUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_user (
            username, password_hash, status, display_name, is_super_admin
        ) VALUES (
            #{username}, #{passwordHash}, #{status}, #{displayName}, #{isSuperAdmin}
        )
    </insert>

    <update id="updateProfile" parameterType="org.example.kah.entity.AdminUser">
        UPDATE admin_user
        SET password_hash = #{passwordHash},
            status = #{status},
            display_name = #{displayName},
            is_super_admin = #{isSuperAdmin}
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginAt">
        UPDATE admin_user
        SET last_login_at = #{loginAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM admin_user
        WHERE id = #{id}
    </delete>
</mapper>